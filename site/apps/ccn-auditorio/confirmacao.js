void import('./brand-signature.js');
const params=new URLSearchParams(location.search),token=params.get('token'),action=params.get('action');
const title=document.querySelector('#confirmation-title'),text=document.querySelector('#confirmation-text'),button=document.querySelector('#confirm-action'),result=document.querySelector('#confirmation-result');
if(!token||!['confirm','cancel'].includes(action)){title.textContent='Link inválido';text.textContent='O link está incompleto ou não é reconhecido.';button.hidden=true;}else{title.textContent=action==='confirm'?'Confirmar reserva':'Desistir da reserva';text.textContent=action==='confirm'?'Confirme que o evento será realizado conforme reservado.':'Ao desistir, o horário será imediatamente liberado para outra solicitação.';button.textContent=action==='confirm'?'CONFIRMAR RESERVA':'DESISTIR E LIBERAR HORÁRIO';if(action==='cancel')button.style.background='#a2293b';button.addEventListener('click',async()=>{button.disabled=true;button.textContent='Processando…';try{const response=await fetch('/api/public/confirmation',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,action})}),body=await response.json();if(!response.ok)throw new Error(body.error);document.querySelector('#confirmation-action').hidden=true;result.hidden=false;result.innerHTML=`<h2>Concluído</h2><p>${body.message}</p><p><a href="/">Voltar para a agenda</a></p>`;}catch(error){result.hidden=false;result.className='result error';result.textContent=error.message;}finally{button.disabled=false;}});}
