void import('./brand-signature.js');
const token=new URLSearchParams(location.search).get('token'),forgot=location.hash==='#esqueci'||!token;document.querySelector('#set-area').hidden=forgot;document.querySelector('#forgot-area').hidden=!forgot;
document.querySelector('#password-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,message=document.querySelector('#password-message'),password=form.elements.password.value;if(password!==form.elements.confirmation.value){message.textContent='As senhas não coincidem.';return;}message.textContent='Salvando…';const response=await fetch('/api/auth/set-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})}),body=await response.json();message.textContent=body.message||body.error;if(response.ok)setTimeout(()=>location.href='/',1200);});
document.querySelector('#forgot-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,message=document.querySelector('#forgot-message');message.textContent='Enviando…';const response=await fetch('/api/auth/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),body=await response.json();message.textContent=body.message||body.error;});
